import Image from "next/image";
import Button from "../_components/ui/button";
import Card from "../_components/ui/card";

const statusIndicator = ({children} : { children: React.ReactNode}) => {
    return(
        <>
        <div>
        yaya
        </div>
        </>

    );
};

const Work = () => {
    return(
        <>
            yaya
        </>
    );
};

export default Work;

